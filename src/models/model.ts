export interface Posts {
    userId: number;
    id: number;
    title: string;
    body: string;
}

export interface EmployeeInterface{
    id?:string,
    name:string,
    position:string,
    age:number

}
